SELECT TOP 1
	CONVERT (INT, ROW_NUMBER() OVER (ORDER BY TEST.ROW DESC)) AS CNT 
FROM TEST TEST
JOIN TEST A ON TEST.TEST = A.TEST
WHERE TEST.TEST IS NOT NULL
GROUP BY TEST.TEST, TEST.TEST2, TEST.TEST3
ORDER BY CNT DESC
